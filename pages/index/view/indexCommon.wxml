<template name="common">

  <image class="burnup" src="../../images/index/burnup.png" wx:if="{{burnupShow}}"></image>

  <view class="fatBurning-part">
  <!--  <image class="home-heratbg" src="../../images/index/heart.png"></image>-->
<!--
    <view class="home-heartbox {{homeHeartBox[homeHeartBoxIndex]}}">
      <view class="heart-hint">
        <image class="{{hintPic?'hint-pic':'hint-pic-animation'}}" src="{{picStateUrl}}" wx:if="{{picState}}" ></image>
        <button class="hint-btn" wx:elif="{{btnState}}" bindgetuserinfo="onGetUserInfoEvent" open-type="getUserInfo"><text>点击绑定</text></button>
        <view class="hint-text">{{textState}}</view>
      </view>
    </view>-->

     <!-- <image class="fatBurning-img" src="{{fatBuringImg}}"></image>-->
      <view wx:if="{{readyimg}}" class="fatBurning-img"></view>

      <view wx:if="{{blowingImg}}" class="blowing-number">{{blowNumber}}</view>
      <view wx:if="{{blowpicShow}}" class="fatBlow-img"></view>
      <view wx:if="{{blowingImg}}" class="blowing-img"></view>
      <view wx:if="{{blowingImg}}" class="blowing-imgs"></view>
      <view class="fatBurning-tip" wx:if="{{homePointHot}}">
          <text>
              设备长时间未使用，为了保证结果准确\n设备还需要些时间预热，请耐心等待
          </text>
      </view>
      <view wx:if="{{disblowImg}}" class="disblow-error"></view>
      <view wx:if="{{disblowImg}}" class="disblow-img"></view>
      <view wx:if="{{disblowImg}}" class="blowing-imgs"></view>



      <view class="fatBurning-text">{{textState}}</view>
      <view class="fatBurning-textEn">{{textStateEn}}</view>

      <view wx:if="{{disblowImg}}" class="fatBurning-tip"><text>吹气不足，短按设备按键重新吹气，吹\n气中请保持匀速，不要忽快忽慢</text></view>

  </view>

  <view class="home-title" wx:if="{{homeTitle}}">{{homeTitleText}}</view>

</template>




